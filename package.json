{
  "name": "workspace",
  "version": "1.0.0",
  "description": "A production-ready Node.js microservice that provides rate limiting functionality using Express.js and PostgreSQL. This service can be deployed independently and used by multiple applications to enforce rate limits based on IP addresses and endpoints.",
  "main": "server.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "node server.js"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "cors": "^2.8.5",
    "dotenv": "^16.5.0",
    "express": "^5.1.0",
    "pg": "^8.16.0",
    "redis": "^5.1.0"
  }
}
