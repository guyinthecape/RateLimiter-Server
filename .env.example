# Railway PostgreSQL Database Configuration
PGHOST=your_railway_host
PGPORT=5432
PGUSER=your_railway_user
PGPASSWORD=your_railway_password
PGDATABASE=your_railway_database

# Application Configuration
PORT=5000
NODE_ENV=development

# Note: This microservice connects to external Railway PostgreSQL
# SSL is always enabled for secure connections
